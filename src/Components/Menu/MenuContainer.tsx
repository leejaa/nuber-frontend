import React from "react";
import MenuPresenter from "./MenuPresenter";

export default () => {

    return (
        <div>
            <MenuPresenter
                // data={data}
                 loading={false}
                // toggleDrivingFn={toggleDrivingFn}
            />
        </div>
    );
};